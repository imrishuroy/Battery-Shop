import 'package:flutter/material.dart';

class InverterExcel extends StatelessWidget {
  const InverterExcel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Will display inverter excel'),
      ),
    );
  }
}
