import 'package:flutter/material.dart';

class BatteryExcel extends StatelessWidget {
  const BatteryExcel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Will display battery excel'),
      ),
    );
  }
}
